var services = {
    cut: "60 грн",
    Shave: "80 грн",
    wash: "100 грн"
    };

    function totalPrice () {
        let totalPrice = 0
        for (let i in services)
totalPrice = totalPrice + parseFloat(services[i]);
return totalPrice
    }
    function maxPrice () {
let maxPrice = 0
for (let i in services)
maxPrice = Math.max(maxPrice, parseFloat(services[i]));
return maxPrice
    }
    function minPrice () {
        let minPrice = Infinity;
        for (let i in services)
        minPrice = Math.min(minPrice, parseFloat(services[i]));
        return minPrice
            }
    console.log(totalPrice())
    console.log(maxPrice())
    console.log(minPrice())




/* Опис ДЗ: Припустимо, у нас є об'єкт. Властивості об'єкта - назва послуги та її вартість:
var services = {
"стрижка": "60 грн",
"гоління": "80 грн",
"Миття голови": "100 грн"
};
"Послуги" можуть додаватися по ходу роботи:
services['Розбити скло'] = "200 грн";
Умови виконання ДЗ
створити метод price(), який обчислює та повертає загальну вартість "наданих послуг".
створити метод minPrice() - який повертає мінімальний price
створити метод maxPrice() - який повертає максимальний price */
